<Window x:Class="GuessMyMessClient.View.HomePages.GuestLoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ingreso Invitado" Height="450" Width="800"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None">

    <Grid>
        <Image Source="/Resources/Images/background.jpeg" Stretch="UniformToFill"/>
        <Rectangle Fill="White" Opacity="0.9" RadiusX="20" RadiusY="20" Margin="200,50,200,50"/>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Ingresar como Invitado" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Correo Electrónico:" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Email}" Height="53" Margin="0,5,0,15" Padding="5"/>

            <TextBlock Text="Código de Invitación:" FontWeight="SemiBold"/>
            <TextBox Text="{Binding InvitationCode}" Height="51" Margin="0,5,0,20" Padding="5"/>

            <Button Command="{Binding LoginGuestCommand}" Content="Ingresar" Height="40" Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
            <Button Command="{Binding BackCommand}" Content="Cancelar" Height="30" Margin="0,10,0,0" Background="Transparent" BorderBrush="Transparent" Foreground="Gray"/>
        </StackPanel>
    </Grid>
</Window>