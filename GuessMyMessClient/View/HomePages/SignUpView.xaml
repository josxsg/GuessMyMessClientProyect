<Window x:Class="GuessMyMessClient.View.HomePages.SignUpView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GuessMyMessClient.View.HomePages"
        mc:Ignorable="d"
        xmlns:helpers="clr-namespace:GuessMyMessClient.ViewModel.Support" 
        xmlns:language="clr-namespace:GuessMyMessClient.Properties.Langs"
        xmlns:vm="clr-namespace:GuessMyMessClient.ViewModel.HomePages"
        d:DataContext="{d:DesignInstance Type=vm:SignUpViewModel, IsDesignTimeCreatable=True}"
        Title="Sign Up" Height="600" Width="950"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        WindowState="Maximized"
        WindowStyle="None">
    <Grid Cursor="">
        <Grid.Background>
            <ImageBrush ImageSource="/Resources/Images/background.jpeg" Stretch="UniformToFill"/>
        </Grid.Background>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,5,0">

            <Button Content="—" Width="30" Height="30" Margin="5" Style="{StaticResource BlueButton}"
                    Command="{Binding MinimizeWindowCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" ToolTip="{x:Static language:Lang.globalBtnMinimize}"/>

            <Button Content="☐" Width="30" Height="30" Margin="5" Style="{StaticResource GreenButton}"
                    Command="{Binding MaximizeWindowCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" ToolTip="{x:Static language:Lang.globalBtnResize}"/>

            <Button Content="X" Width="30" Height="30" Margin="5" Style="{StaticResource RedButton}"
                    Command="{Binding CloseWindowCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" ToolTip="{x:Static language:Lang.globalBtnClose}"/>

        </StackPanel>

        <Viewbox Stretch="Uniform" MaxWidth="800">
            <StackPanel Height="416" Width="961">
                <TextBlock Text="{x:Static language:Lang.signUpLbResgister}" FontSize="28" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="10">
                        <TextBlock Text="{x:Static language:Lang.globalTxtName}" HorizontalAlignment="Left" Margin="160,0,0,5" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="{x:Static language:Lang.globalTxtUsername}" HorizontalAlignment="Left" Margin="130,15,0,5" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="{x:Static language:Lang.globalTxtEmail}" HorizontalAlignment="Left" Margin="130,15,0,5" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="10">
                        <TextBlock Text="{x:Static language:Lang.globalLbLastName}" HorizontalAlignment="Left" Margin="160,0,0,5" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="{x:Static language:Lang.globalLbGender}" HorizontalAlignment="Left" Margin="160,15,0,5" FontWeight="SemiBold" Cursor="Hand"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Cursor="">
                            <RadioButton GroupName="Gender" IsChecked="{Binding IsMale}" Style="{StaticResource GenderRadioButtonStyle}" ToolTip="{x:Static language:Lang.globalCmbMale}">
                                <Image Source="/Resources/Images/male.png" Height="25"/>
                            </RadioButton>
                            <RadioButton Content="{x:Static language:Lang.globalCmbNonBinary}" GroupName="Gender" IsChecked="{Binding IsNonBinary}" Style="{StaticResource GenderRadioButtonStyle}" Margin="5,0"/>
                            <RadioButton GroupName="Gender" IsChecked="{Binding IsFemale}" Style="{StaticResource GenderRadioButtonStyle}" ToolTip="{x:Static language:Lang.globalCmbFemale}">
                                <Image Source="/Resources/Images/female.png" Height="25"/>
                            </RadioButton>
                        </StackPanel>

                        <TextBlock Text="{x:Static language:Lang.globalPwdPassword}" HorizontalAlignment="Left" Margin="150,15,0,5" FontWeight="SemiBold"/>
                        <PasswordBox helpers:PasswordBoxHelper.Attach="True"
                                     helpers:PasswordBoxHelper.Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource InputField}"/>
                    </StackPanel>

                    <Grid Grid.Column="1" VerticalAlignment="Top" Margin="20,40" Width="150" Height="150">
                        <Ellipse Fill="LightGray" Stroke="Black" StrokeThickness="2"/>

                        <Image Source="{Binding SelectedAvatarImage}" Stretch="Uniform" Margin="10"/>

                        <Button Style="{StaticResource EditButtonStyle}" Width="40" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,-5,-5"
                                Command="{Binding SelectAvatarCommand}"
                                ToolTip="{x:Static language:Lang.editAvatarLbSelect}"/>
                    </Grid>
                </Grid>

                <Button Content="{x:Static language:Lang.globalBtnSignUp}" 
                        Style="{StaticResource YellowButton}" 
                        Command="{Binding SignUpCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            </StackPanel>
        </Viewbox>
    </Grid>
</Window>
