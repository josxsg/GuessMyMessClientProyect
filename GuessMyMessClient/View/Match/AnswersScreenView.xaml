<Window x:Class="GuessMyMessClient.View.Match.AnswersScreenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GuessMyMessClient.View.Match"
        mc:Ignorable="d"
        Title="AnswersScreenView" Height="550" Width="900"
        xmlns:vm="clr-namespace:GuessMyMessClient.ViewModel.Match"
        d:DataContext="{d:DesignInstance Type=vm:AnswersScreenViewModel, IsDesignTimeCreatable=True}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        xmlns:language="clr-namespace:GuessMyMessClient.Properties.Langs">

    <Window.Resources>
        <DataTemplate x:Key="PlayerListItemTemplate">
            <TextBlock Text="{Binding Username}" FontSize="14" Foreground="Black" />
        </DataTemplate>

        <DataTemplate x:Key="ChatMessageItemTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Sender}" FontWeight="Bold" Foreground="DarkBlue" />
                <TextBlock Text=": " />
                <TextBlock Text="{Binding Message}" Foreground="Black" />
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Resources/Images/background.jpeg" Stretch="UniformToFill"/>
        </Grid.Background>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="1*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="2*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="{x:Static language:Lang.answersPanelLbOwner}"
                       FontSize="24" 
                       FontWeight="Bold" />

            <TextBlock Grid.Row="1"
                       Text="{Binding DrawingArtistName}" 
                       FontSize="20"
                       Foreground="DarkGoldenrod"
                       Margin="0,0,0,15"/>

            <TextBlock Grid.Row="2"
                       Text="{x:Static language:Lang.globalLbPlayers}"
                       FontSize="24" 
                       FontWeight="Bold" />

            <ListBox Grid.Row="3"
                     ItemsSource="{Binding PlayerList}"
                     ItemTemplate="{StaticResource PlayerListItemTemplate}"
                     Background="#AAFFFFFF"
                     Margin="0,5,0,15"/>

            <TextBlock Grid.Row="4"
                       Text="Chat"
                       FontSize="24" 
                       FontWeight="Bold" />

            <ListBox Grid.Row="5"
                     ItemsSource="{Binding ChatMessages}"
                     ItemTemplate="{StaticResource ChatMessageItemTemplate}"
                     Background="#AAFFFFFF"
                     Margin="0,5,0,0"/>

            <Grid Grid.Row="6" Margin="0,10,0,0" VerticalAlignment="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox
                    Grid.Column="0"
                    Text="{Binding NewChatMessage, UpdateSourceTrigger=PropertyChanged}"
                    Style="{StaticResource TextBox}"
                    MinHeight="30"
                    VerticalAlignment="Center"/>
                <Button 
                    Grid.Column="1"
                    Content="{x:Static language:Lang.globalBtnSend}" 
                    Command="{Binding SendMessageCommand}"
                    Style="{StaticResource JoinButton}" 
                    FontSize="18"
                    Height="30"
                    MinWidth="60"
                    Margin="5,0,0,0"
                    VerticalAlignment="Center"/>
            </Grid>

        </Grid>

        <Grid Grid.Column="1" Margin="10,20,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" BorderBrush="Black" BorderThickness="2" CornerRadius="5">
                <Image Source="{Binding CurrentDrawing}" Stretch="Uniform" />
            </Border>

            <TextBlock Grid.Row="1"
                       Text="Respuestas"
                       FontSize="24" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0" />

            <Border Grid.Row="2" Background="#AAFFFFFF" Margin="0,5,0,0" CornerRadius="5">
                <TextBlock Text="{Binding CurrentDisplayedGuess}"
                           FontSize="28"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"/>
            </Border>
        </Grid>

    </Grid>
</Window>