<Window x:Class="GuessMyMessClient.View.Lobby.SelectAvatarView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:GuessMyMessClient.ViewModel.Lobby"
        xmlns:local="clr-namespace:GuessMyMessClient.View.Lobby"
        xmlns:language ="clr-namespace:GuessMyMessClient.Properties.Langs"
        mc:Ignorable="d"
        Title="Select Avatar" Height="550" Width="900"
        d:DataContext="{d:DesignInstance Type=vm:SelectAvatarViewModel, IsDesignTimeCreatable=True}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True">

    <Border Background="White" BorderBrush="#FF42A5F5" BorderThickness="3" CornerRadius="20" Padding="20" Margin="20">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="10" Color="Black" Opacity="0.5" BlurRadius="30"/>
        </Border.Effect>

        <Grid>
            <Button HorizontalAlignment="Right" VerticalAlignment="Top" 
                    Content="X" Width="35" Height="35" Margin="0,-10,-10,0"
                    Style="{StaticResource RedButton}"
                    Command="{Binding CloseCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" 
                    ToolTip="{x:Static language:Lang.globalBtnClose}"/>

            <StackPanel Margin="0,30,0,0">

                <Border Style="{StaticResource GlassTitleBorder}">
                    <TextBlock Text="{x:Static language:Lang.editAvatarLbSelect}" 
                               Style="{StaticResource GlassTitleText}"/>
                </Border>

                <ItemsControl ItemsSource="{Binding AvailableAvatars}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Center" MaxWidth="700"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.SelectAvatarItemCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}"
                                    CommandParameter="{Binding}"
                                    Margin="15" Width="90" Height="90"
                                    ToolTip="{Binding Name}" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Grid>
                                            <Border x:Name="border" CornerRadius="45" Background="#F5F5F5" BorderBrush="#CFD8DC" BorderThickness="3">
                                                <Border.Effect>
                                                    <DropShadowEffect ShadowDepth="2" Color="Black" Opacity="0.2" BlurRadius="5"/>
                                                </Border.Effect>
                                                <Border CornerRadius="42" ClipToBounds="True">
                                                    <Image Source="{Binding ImageSource}" Stretch="Uniform" Margin="8"/>
                                                </Border>
                                            </Border>

                                            <Border x:Name="CheckMark" Background="#FF2ECC71" CornerRadius="12" Width="24" Height="24" 
                                                    HorizontalAlignment="Right" VerticalAlignment="Bottom" Visibility="Collapsed" BorderBrush="White" BorderThickness="2">
                                                <TextBlock Text="✔" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" FontWeight="Bold"/>
                                            </Border>
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="#FF42A5F5"/>
                                            </Trigger>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="#FFC107"/>
                                                <Setter TargetName="border" Property="BorderThickness" Value="5"/>
                                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Button Content="{x:Static language:Lang.globalBtnConfirm}" 
                        Style="{StaticResource JoinButton}" 
                        Command="{Binding ConfirmSelectionCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Margin="0,30,0,0" Width="200" Height="50" FontSize="20"/>
            </StackPanel>
        </Grid>

    </Border>
</Window>