<UserControl x:Class="GuessMyMessClient.View.Lobby.FriendsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GuessMyMessClient.View.Lobby"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:GuessMyMessClient.Properties.Langs"
        xmlns:vm="clr-namespace:GuessMyMessClient.ViewModel.Lobby"
        d:DataContext="{d:DesignInstance Type=vm:FriendsViewModel, IsDesignTimeCreatable=True}"
        d:DesignHeight="450" d:DesignWidth="300">
    <Border Background="White" BorderBrush="Black" BorderThickness="2" CornerRadius="10" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" 
                         Style="{StaticResource SearchTextBoxStyle}"
                         Tag="{x:Static language:Lang.socialTfSearchPlayers}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Column="1" 
                        Style="{StaticResource BlueIconButton}" 
                        Command="{Binding SearchCommand}" 
                        ToolTip="{x:Static language:Lang.socialBtnSearch}"
                        Margin="5,0,0,0">
                    <Image Source="/Resources/Images/search.png" Height="20"/>
                </Button>
            </Grid>

            <ListBox Grid.Row="1" ItemsSource="{Binding SearchResults}" Margin="0,10,0,0"
                     BorderThickness="1" BorderBrush="LightGray" MaxHeight="120"
                     HorizontalContentAlignment="Stretch" Visibility="{Binding SearchResults.Count, Converter={StaticResource CountToVisibilityConverter}}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Username}" VerticalAlignment="Center"/>

                            <Button Grid.Column="1" 
                                    Content="{x:Static language:Lang.socialBtnAddFriend}" 
                                    Command="{Binding DataContext.SendFriendRequestCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource YellowButton}"
                                    HorizontalAlignment="Right" 
                                    Width="125" 
                                    Height="35"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>


            <TabControl Grid.Row="2" Margin="0,10,0,0">
                <TabItem Header="{x:Static language:Lang.socialLbFriends}">
                    <ListBox ItemsSource="{Binding Friends}" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="5">
                                    <Ellipse Width="10" Height="10" Fill="{Binding IsOnline, Converter={StaticResource BoolToGreenRedConverter}}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Username}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </TabItem>
                <TabItem Header="{x:Static language:Lang.socialLbRequests}">
                    <ListBox ItemsSource="{Binding FriendRequests}" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding RequesterUsername}" VerticalAlignment="Center"/>

                                    <Button Grid.Column="1" Content="Accept" Margin="5,0" Command="{Binding AcceptCommand}"/>
                                    <Button Grid.Column="2" Content="Decline" Command="{Binding DeclineCommand}"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </TabItem>
            </TabControl>

            <Button Grid.Row="3" Style="{StaticResource YellowButton}" Command="{Binding InviteByEmailCommand}" VerticalAlignment="Bottom" HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/Images/mail.png" Height="20" Margin="0,0,10,0"/>
                    <TextBlock Text="{x:Static language:Lang.socialBtnEmailInivite}" VerticalAlignment="Center" FontSize="10"/>
                </StackPanel>
            </Button>
        </Grid>
    </Border>
</UserControl>
