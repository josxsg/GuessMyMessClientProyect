<Window x:Class="GuessMyMessClient.View.Lobby.ProfileView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GuessMyMessClient.View.Lobby"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:GuessMyMessClient.Properties.Langs"
        xmlns:vm="clr-namespace:GuessMyMessClient.ViewModel.Lobby"
        d:DataContext="{d:DesignInstance Type=vm:ProfileViewModel, IsDesignTimeCreatable=True}"
        d:DesignHeight="450" d:DesignWidth="300">
    <Border Background="White" BorderBrush="Black" BorderThickness="2" CornerRadius="10" Padding="15">
        <StackPanel>
            <TextBlock Text="{x:Static language:Lang.editProfileLbMyProfile}" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="{x:Static language:Lang.globalLbName}" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Name}" IsReadOnly="False" Margin="0,2,0,10" Width="250"/>

            <TextBlock Text="{x:Static language:Lang.globalLbLastName}" FontWeight="SemiBold"/>
            <TextBox Text="{Binding LastName}" IsReadOnly="False" Margin="0,2,0,10" Width="250"/>

            <TextBlock Text="{x:Static language:Lang.globalLbEmail}" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Email}" IsReadOnly="True" Margin="0,2,0,10" Width="250"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,20">
                <RadioButton GroupName="ProfileGender" IsChecked="{Binding IsMale}" Style="{StaticResource GenderRadioButtonStyle}" IsEnabled="False">
                    <Image Source="/Resources/Images/male.png" Height="25"/>
                </RadioButton>
                <RadioButton Content="{x:Static language:Lang.globalCmbNonBinary}" GroupName="ProfileGender" IsChecked="{Binding IsNonBinary}" Style="{StaticResource GenderRadioButtonStyle}" Margin="5,0" IsEnabled="False"/>
                <RadioButton GroupName="ProfileGender" IsChecked="{Binding IsFemale}" Style="{StaticResource GenderRadioButtonStyle}" IsEnabled="False">
                    <Image Source="/Resources/Images/female.png" Height="25"/>
                </RadioButton>
            </StackPanel>

            <Button Content="{x:Static language:Lang.editProfileBtnChangeEmail}" 
                    Style="{StaticResource YellowButton}" 
                    Command="{Binding ChangeEmailCommand}" Height="28" Width="230"/>

            <Button Content="{x:Static language:Lang.editProfileBtnChangePsw}" 
                    Style="{StaticResource BlueButton}" 
                    Command="{Binding ChangePasswordCommand}" Height="28" Width="230"/>

        </StackPanel>
    </Border>
</Window>
