<UserControl x:Class="GuessMyMessClient.View.Lobby.DirectMessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GuessMyMessClient.View.Lobby"
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:GuessMyMessClient.ViewModel.Lobby"
             d:DataContext="{d:DesignInstance Type=vm:DirectMessageViewModel, IsDesignTimeCreatable=True}"
             xmlns:language ="clr-namespace:GuessMyMessClient.Properties.Langs"
             Width="500" Height="650" d:DesignHeight="650" d:DesignWidth="500">

    <Border Background="White" BorderBrush="#FF42A5F5" BorderThickness="3" CornerRadius="20" Padding="15">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="5" Color="Black" Opacity="0.4" BlurRadius="20"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ComboBox Grid.Row="0"
                      Height="45"
                      Margin="0,0,0,10"
                      ItemsSource="{Binding Conversations}"
                      SelectedItem="{Binding SelectedConversation, Mode=TwoWay}"
                      Style="{StaticResource ComboBoxStyle}"
                      Tag="{x:Static language:Lang.socialTfSearchPlayers}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Username}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <Border Grid.Row="1" Background="#F5F5F5" CornerRadius="10" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,5,0,10">
                <ListView ItemsSource="{Binding ChatHistory}" 
                          HorizontalContentAlignment="Stretch"
                          SelectedItem="{Binding SelectedMessage, Mode=TwoWay}" 
                          BorderThickness="0" 
                          Background="Transparent"
                          Padding="5">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="8" Padding="10" Margin="5,2">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="1" Color="#DDD" BlurRadius="2"/>
                                </Border.Effect>
                                <StackPanel>
                                    <TextBlock Text="{Binding SenderUsername}" FontWeight="Bold" FontFamily="Arial Rounded MT Bold" Foreground="#FF1565C0" FontSize="14"/>
                                    <TextBlock Text="{Binding Content}" TextWrapping="Wrap" FontFamily="Arial" FontSize="14" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Height="45"
                         Margin="0,0,10,0" 
                         Style="{StaticResource InputField}"
                         Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Grid.Column="1"
                        Content="{x:Static language:Lang.globalBtnSend}" 
                        Style="{StaticResource JoinButton}" 
                        FontSize="16"
                        Width="90" 
                        Height="45"
                        Command="{Binding SendMessageCommand}"/>
            </Grid>

        </Grid>
    </Border>
</UserControl>