<Window x:Class="GuessMyMessClient.View.Lobby.ConfigurationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GuessMyMessClient.View.Lobby"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:GuessMyMessClient.Properties.Langs"
        xmlns:vm="clr-namespace:GuessMyMessClient.ViewModel.Lobby"
        d:DataContext="{d:DesignInstance Type=vm:ConfigurationViewModel, IsDesignTimeCreatable=True}"
        d:DesignHeight="450" d:DesignWidth="300">
    <Border Background="White" BorderBrush="Black" BorderThickness="2" CornerRadius="10" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{x:Static language:Lang.configLbConfiguration}" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <StackPanel Grid.Row="1">
                <TextBlock Text="{x:Static language:Lang.configLbSound}" FontSize="18" FontWeight="Bold"/>
                <Grid Margin="10,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{x:Static language:Lang.configLbSoundEffects}" VerticalAlignment="Center"/>
                    <ToggleButton Grid.Column="1" IsChecked="{Binding SoundEffectsEnabled}" Style="{StaticResource ToggleSwitchStyle}"/>
                </Grid>
                <Grid Margin="10,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{x:Static language:Lang.configLbMusic}" VerticalAlignment="Center"/>
                    <ToggleButton Grid.Column="1" IsChecked="{Binding MusicEnabled}" Style="{StaticResource ToggleSwitchStyle}"/>
                </Grid>
                <Grid Margin="10,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{x:Static language:Lang.configLbVolume}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Slider Grid.Column="1" Value="{Binding VolumeLevel}" Maximum="100" VerticalAlignment="Center"/>
                </Grid>

                <TextBlock Text="{x:Static language:Lang.configLbLanguage}" FontSize="18" FontWeight="Bold" Margin="0,20,0,0"/>
                <StackPanel Orientation="Horizontal" Margin="10,5">
                    <RadioButton Content="{x:Static language:Lang.configBtnSpanish}" GroupName="Language" IsChecked="{Binding IsSpanish}" Style="{StaticResource LanguageRadioButtonStyle}"/>
                    <RadioButton Content="{x:Static language:Lang.configBtnEnglish}" GroupName="Language" IsChecked="{Binding IsEnglish}" Style="{StaticResource LanguageRadioButtonStyle}" Margin="5,0"/>
                </StackPanel>
            </StackPanel>

            <Button Grid.Row="2" Style="{StaticResource RedButtonStyle}" Command="{Binding LogoutCommand}" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/Images/logout.png" Height="20" Margin="0,0,10,0" Width="26"/>
                    <TextBlock Text="{x:Static language:Lang.configtBtnLogOut}" VerticalAlignment="Center" Height="23" Width="105"/>
                </StackPanel>
            </Button>
        </Grid>
    </Border>
</Window>
