<Window x:Class="GuessMyMessClient.View.Lobby.Dialogs.AddSocialNetworkView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:language="clr-namespace:GuessMyMessClient.Properties.Langs"
                xmlns:vm="clr-namespace:GuessMyMessClient.ViewModel.Lobby.Dialogs" 
                d:DataContext="{d:DesignInstance Type=vm:AddSocialNetworkViewModel, IsDesignTimeCreatable=True}"
        mc:Ignorable="d"
        Title="Agregar Red Social" 
        Height="300" Width="400"
        WindowStyle="None" 
        ResizeMode="NoResize" 
        AllowsTransparency="True" 
        Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Border Background="White" 
            BorderBrush="#FF42A5F5" 
            BorderThickness="3" 
            CornerRadius="20" 
            Padding="20">

        <Border.Effect>
            <DropShadowEffect ShadowDepth="5" Color="Black" Opacity="0.4" BlurRadius="20"/>
        </Border.Effect>

        <Grid>

            <Button Style="{StaticResource CloseButtonStyle}" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Top"
                    Command="{Binding CloseCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

                <Border Style="{StaticResource PopupSubtitleBorder}" Margin="0,10,0,0">
                    <TextBlock Text="{x:Static language:Lang.socialLbProfileLink}" Style="{StaticResource PopupSubtitleText}"/>
                </Border>

                <TextBox Text="{Binding UserLink, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource InputField}"
                         Width="300" Height="40" Margin="0,5,0,20"
                         ToolTip="Añadir enlace a red social"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="{x:Static language:Lang.globalBtnConfirm}" 
                            Command="{Binding ConfirmCommand}" 
                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                            Style="{StaticResource GreenButton}" 
                            Width="120" Height="35"/>
                </StackPanel>

            </StackPanel>
        </Grid>
    </Border>
</Window>