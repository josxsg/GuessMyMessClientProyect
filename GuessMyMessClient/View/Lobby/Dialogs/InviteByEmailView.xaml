<Window x:Class="GuessMyMessClient.View.Lobby.Dialogs.InviteByEmailView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:language="clr-namespace:GuessMyMessClient.Properties.Langs"
        xmlns:vm="clr-namespace:GuessMyMessClient.ViewModel.Lobby.Dialogs" mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:InviteByEmailViewModel, IsDesignTimeCreatable=True}"
        Style="{StaticResource DialogWindowStyle}"
        WindowStartupLocation="CenterScreen" Height="200" Width="400">
    <Border Background="White" BorderBrush="Black" BorderThickness="2" CornerRadius="10">
        <Grid Margin="15">
            <Button Style="{StaticResource CloseButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Command="{Binding CloseCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

            <StackPanel>
                <TextBlock Text="{x:Static language:Lang.socialLbEmailInvitation}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBox Style="{StaticResource SearchTextBoxStyle}" Tag="{x:Static language:Lang.globalTxtEmail}"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,20"/>
                <Button Content="{x:Static language:Lang.socialBtnSendInvite}" Style="{StaticResource BlueButton}" Command="{Binding SendInviteCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>