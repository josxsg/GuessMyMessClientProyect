<Window x:Class="GuessMyMessClient.View.Lobby.Dialogs.VerifyChangesByCodeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:language="clr-namespace:GuessMyMessClient.Properties.Langs"
        xmlns:vm="clr-namespace:GuessMyMessClient.ViewModel.Lobby.Dialogs" mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:VerifyChangesByCodeViewModel, IsDesignTimeCreatable=True}"
        Style="{StaticResource DialogWindowStyle}"
        WindowStartupLocation="CenterScreen" Height="280" Width="450">
    <Border Background="White" BorderBrush="Black" BorderThickness="2" CornerRadius="10">
        <Grid Margin="15">
            <Button Style="{StaticResource CloseButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Command="{Binding CloseCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="{x:Static language:Lang.changePswVerifyingLbVerifying}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="{x:Static language:Lang.globalLbVerifyinText}" Margin="0,20,0,5" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                <TextBox Style="{StaticResource InputField}"
                         Text="{Binding VerificationCode, UpdateSourceTrigger=PropertyChanged}"
                         MaxLength="6"
                         TextAlignment="Center" FontSize="24" FontWeight="Bold" Foreground="Gray" Height="60"/>
                <Button Content="{x:Static language:Lang.globalBtnVerify}" Style="{StaticResource BlueButton}" Command="{Binding VerifyCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Margin="0,20,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>