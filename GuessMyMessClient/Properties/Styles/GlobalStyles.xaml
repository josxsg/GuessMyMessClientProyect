<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="MainButton" TargetType="Button">
        <Setter Property="Background" Value="#FFD5A100"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Padding" Value="15,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="20"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">

                        <Border.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1" CenterX="167" CenterY="22.5" />
                        </Border.RenderTransform>

                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#FFF2B800"/>
                        </Trigger>

                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="border"
                                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                     From="1" To="1.05" Duration="0:0:1.5"
                                                     AutoReverse="True" RepeatBehavior="Forever"/>

                                    <DoubleAnimation Storyboard.TargetName="border"
                                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                     From="1" To="1.05" Duration="0:0:1.5"
                                                     AutoReverse="True" RepeatBehavior="Forever"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MainButton2" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontWeight" Value="Black"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid RenderTransformOrigin="0.5,0.5">

                        <Border x:Name="glow" CornerRadius="25" Background="#FFFFC107" Opacity="0.5" Margin="0,4,0,0">
                            <Border.Effect>
                                <BlurEffect Radius="15"/>
                            </Border.Effect>
                        </Border>

                        <Border x:Name="border"
                            CornerRadius="25"
                            BorderBrush="#CC8500" 
                            BorderThickness="1.5">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#FFFFE082" Offset="0.0"/>
                                    <GradientStop Color="#FFFFA000" Offset="1.0"/>
                                </LinearGradientBrush>
                            </Border.Background>

                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" />
                            </Border.RenderTransform>

                            <Grid>
                                <Border x:Name="glossOverlay" Height="22" VerticalAlignment="Top" Margin="0" CornerRadius="24,24,10,10" Opacity="0.5">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#DDFFFFFF" Offset="0"/>
                                            <GradientStop Color="#00FFFFFF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>

                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0">
                                    <ContentPresenter.Effect>
                                        <DropShadowEffect ShadowDepth="2" Direction="315" Color="#8B5A00" Opacity="0.6" BlurRadius="2"/>
                                    </ContentPresenter.Effect>
                                </ContentPresenter>
                            </Grid>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#FFFFECB3" Offset="0.0"/>
                                        <GradientStop Color="#FFFFB300" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="glow" Property="Opacity" Value="0.8"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.96" ScaleY="0.96" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" From="1" To="1.03" Duration="0:0:1.5" AutoReverse="True" RepeatBehavior="Forever"/>
                                    <DoubleAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="1" To="1.03" Duration="0:0:1.5" AutoReverse="True" RepeatBehavior="Forever"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InstructionText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="FontSize" Value="100"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Margin" Value="0,0,0,30"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect ShadowDepth="2" 
                                  Direction="315" 
                                  Color="#FF333333" 
                                  Opacity="1" 
                                  BlurRadius="0"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MenuButtonBase" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="19"/>
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Height" Value="50"/>
        <Setter Property="Width" Value="260"/>
        <Setter Property="Margin" Value="0,10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid RenderTransformOrigin="0.5,0.5">

                        <Border x:Name="shadow"
                            CornerRadius="15"
                            Background="{TemplateBinding BorderBrush}" 
                            Margin="0,6,0,0"/>

                        <Border x:Name="face"
                            CornerRadius="15"
                            Background="{TemplateBinding Background}"
                            Margin="0,0,0,6">

                            <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="face" Property="Opacity" Value="0.9"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="face" Property="Margin" Value="0,6,0,0"/>
                            <Setter TargetName="shadow" Property="Visibility" Value="Collapsed"/>
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <TranslateTransform Y="1"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="YellowButton" TargetType="Button" BasedOn="{StaticResource MenuButtonBase}">
        <Setter Property="Background" Value="#FFFFC107"/>
        <Setter Property="BorderBrush" Value="#FFC69200"/>
    </Style>

    <Style x:Key="GreenButton" TargetType="Button" BasedOn="{StaticResource MenuButtonBase}">
        <Setter Property="Background" Value="#FF4CAF50"/>
        <Setter Property="BorderBrush" Value="#FF2E7D32"/>
    </Style>

    <Style x:Key="BlueButton" TargetType="Button" BasedOn="{StaticResource MenuButtonBase}">
        <Setter Property="Background" Value="#FF42A5F5"/>
        <Setter Property="BorderBrush" Value="#FF1565C0"/>
    </Style>

    <Style x:Key="RedButton" TargetType="Button" BasedOn="{StaticResource MenuButtonBase}">
        <Setter Property="Background" Value="#FFF44336"/>
        <Setter Property="BorderBrush" Value="#FFC62828"/>
    </Style>

    <Style x:Key="InviteButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#FF2196F3"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Height" Value="45"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid RenderTransformOrigin="0.5,0.5">

                        <Border x:Name="shadow"
                            CornerRadius="20"
                            Background="#FF1565C0" 
                            Margin="0,4,0,0"/>

                        <Border x:Name="face"
                            CornerRadius="20"
                            Background="{TemplateBinding Background}"
                            Margin="0,0,0,4">

                            <Viewbox Stretch="Uniform" Margin="15,5">
                                <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                            </Viewbox>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="face" Property="Background" Value="#FF42A5F5"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="face" Property="Margin" Value="0,4,0,0"/>
                            <Setter TargetName="shadow" Property="Visibility" Value="Collapsed"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="face" Property="Background" Value="#FFBDBDBD"/>
                            <Setter TargetName="shadow" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="face" Property="Margin" Value="0,4,0,0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="ConfirmButton" TargetType="Button">
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="FontFamily" Value="Arial Black"/>
        <Setter Property="FontWeight" Value="Heavy"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Cursor" Value="Hand"/>

        <Setter Property="Background" Value="#F0F0F0"/>
        <Setter Property="BorderBrush" Value="Black"/>
        <Setter Property="BorderThickness" Value="3"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                        CornerRadius="12" 
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">

                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#DDDDDD"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TabButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SelectedTabButton" TargetType="Button" BasedOn="{StaticResource TabButton}">
        <Setter Property="Background" Value="Black"/>
        <Setter Property="Foreground" Value="White"/>
    </Style>

    <Style x:Key="JoinButton" TargetType="Button" BasedOn="{StaticResource MenuButtonBase}">
        <Setter Property="Background" Value="#FF2ECC71"/>
        <Setter Property="BorderBrush" Value="#FF219150"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="Black"/>
        <Setter Property="FontSize" Value="22"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <Style x:Key="LeaveButtonStylee" TargetType="Button" BasedOn="{StaticResource MenuButtonBase}">
        <Setter Property="Background" Value="#FFFF5252"/>
        <Setter Property="BorderBrush" Value="#FFC62828"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="22"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <Style x:Key="WordToSelect" TargetType="Button" BasedOn="{StaticResource MenuButtonBase}">
        <Setter Property="Background" Value="#AB47BC"/>
        <Setter Property="BorderBrush" Value="#7B1FA2"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="Black"/>
        <Setter Property="Height" Value="60"/>
        <Setter Property="Width" Value="280"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="Cursor" Value="Hand"/>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#BA68C8"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TextBox" TargetType="TextBox">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="5,0"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border CornerRadius="8"
                        BorderBrush="Black"
                        BorderThickness="3"
                        Background="White">
                        <ScrollViewer x:Name="PART_ContentHost" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="40" />
            </Grid.ColumnDefinitions>

            <Border x:Name="Border" 
                Grid.ColumnSpan="2"
                CornerRadius="25"
                Background="White"
                BorderBrush="#FF42A5F5"
                BorderThickness="3" 
                Margin="0,0,0,4"/>

            <Path x:Name="Arrow"
              Grid.Column="1"     
              Fill="#FF42A5F5"
              HorizontalAlignment="Center"
              VerticalAlignment="Center"
              Data="M 0 0 L 6 6 L 12 0 Z"
              Margin="0,0,0,4"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter TargetName="Border" Property="Background" Value="#F0F8FF" />
            </Trigger>
            <Trigger Property="IsChecked" Value="true">
                <Setter TargetName="Border" Property="BorderBrush" Value="#FFC107" />
                <Setter TargetName="Border" Property="Background" Value="#FFFDE7" />
                <Setter TargetName="Arrow" Property="Fill" Value="#FFC107" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="ComboBoxItemStyle" TargetType="ComboBoxItem">
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0" Padding="{TemplateBinding Padding}" CornerRadius="5" Margin="2">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#F0F8FF"/>
                            <Setter TargetName="Bd" Property="TextBlock.Foreground" Value="#FF1565C0"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#FF42A5F5"/>
                            <Setter TargetName="Bd" Property="TextBlock.Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="Foreground" Value="#FF424242"/>
        <Setter Property="Height" Value="50"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="15,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItemStyle}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border x:Name="shadow"
                            CornerRadius="25"
                            Background="#FF1565C0" 
                            Margin="0,4,0,0"
                            Opacity="0.6"/>
                        <ToggleButton x:Name="ToggleButton" 
                                  Template="{StaticResource ComboBoxToggleButton}" 
                                  Grid.Column="2" 
                                  Focusable="false"
                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                  ClickMode="Press"/>

                        <ContentPresenter x:Name="ContentSite"
                                      IsHitTestVisible="False" 
                                      Content="{TemplateBinding SelectionBoxItem}"
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                      Margin="15,0,40,4" 
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left"/>

                        <TextBlock Text="{TemplateBinding Tag}" 
                               Foreground="Gray" 
                               Margin="15,0,0,4" 
                               VerticalAlignment="Center"
                               IsHitTestVisible="False"
                               FontStyle="Italic">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedItem, RelativeSource={RelativeSource TemplatedParent}}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Popup x:Name="Popup"
                           Placement="Bottom"
                           IsOpen="{TemplateBinding IsDropDownOpen}"
                           AllowsTransparency="True" 
                           Focusable="False"
                           PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                              MinWidth="{TemplateBinding ActualWidth}"
                              MaxHeight="{TemplateBinding MaxDropDownHeight}"
                              Margin="0,5,0,0">
                                <Border x:Name="DropDownBorder"
                                    Background="White"
                                    BorderBrush="#FF42A5F5"
                                    BorderThickness="2"
                                    CornerRadius="10"
                                    Padding="5">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="4" Color="Black" Opacity="0.2" BlurRadius="10"/>
                                    </Border.Effect>
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="TextBlock" TargetType="Label">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="5,0"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Label">
                    <Border CornerRadius="8"
                        BorderBrush="Black"
                        BorderThickness="3"
                        Background="White"
                        Padding="{TemplateBinding Padding}">

                        <ContentPresenter VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GlassTitleBorder" TargetType="Border">
        <Setter Property="Background" Value="#AA000000"/>
        <Setter Property="CornerRadius" Value="20"/>
        <Setter Property="Padding" Value="30,15"/>
        <Setter Property="Margin" Value="10,0,10,30"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="MaxWidth" Value="880"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect ShadowDepth="4" Color="Black" Opacity="0.3" BlurRadius="10"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GlassTitleText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="FontSize" Value="40"/>
        <Setter Property="FontWeight" Value="Black"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect ShadowDepth="3" Direction="320" Color="Black" Opacity="0.8" BlurRadius="2"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="FormLabelBorder" TargetType="Border">
        <Setter Property="Background" Value="#90000000"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="12,4"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Margin" Value="15,0,0,5"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect ShadowDepth="2" Color="Black" Opacity="0.2" BlurRadius="4"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="FormLabelText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="PopupTitleBorder" TargetType="Border">
        <Setter Property="Background" Value="#AA000000"/>
        <Setter Property="CornerRadius" Value="15"/>
        <Setter Property="Padding" Value="25,10"/>
        <Setter Property="Margin" Value="0,0,0,20"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect ShadowDepth="3" Color="Black" Opacity="0.3" BlurRadius="8"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PopupTitleText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontWeight" Value="Black"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect ShadowDepth="2" Direction="320" Color="Black" Opacity="0.8" BlurRadius="2"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PopupSubtitleBorder" TargetType="Border">
        <Setter Property="Background" Value="#80000000"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="15,5"/>
        <Setter Property="Margin" Value="0,15,0,5"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect ShadowDepth="2" Color="Black" Opacity="0.2" BlurRadius="4"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PopupSubtitleText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="DrawingBoardBlack" TargetType="Label">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="5,0"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Label">
                    <Border CornerRadius="8"
                    BorderBrush="White"
                    BorderThickness="3"
                    Background="Black"
                    Padding="{TemplateBinding Padding}">

                        <ContentPresenter VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="InputField" TargetType="TextBox">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#FF42A5F5"/>
        <Setter Property="BorderThickness" Value="3"/>
        <Setter Property="Foreground" Value="#FF424242"/>
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Padding" Value="15,0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Height" Value="50"/>
        <Setter Property="Width" Value="350"/>
        <Setter Property="CaretBrush" Value="#FFC107"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid>
                        <Border x:Name="shadow"
                            CornerRadius="25"
                            Background="#FF1565C0" 
                            Margin="0,4,0,0"
                            Opacity="0.6"/>

                        <Border x:Name="border"
                            CornerRadius="25"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Margin="0,0,0,4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                      Margin="0"
                                      VerticalAlignment="Center"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#F0F8FF"/>
                        </Trigger>

                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="BorderBrush" Value="#FFC107"/>
                            <Setter TargetName="border" Property="Background" Value="#FFFDE7"/>
                            <Setter TargetName="shadow" Property="Background" Value="#FFA000"/>
                            <Setter TargetName="shadow" Property="Opacity" Value="0.8"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="#EEEEEE"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#BDBDBD"/>
                            <Setter Property="Foreground" Value="Gray"/>
                            <Setter TargetName="shadow" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="border" Property="Margin" Value="0,4,0,0"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="TextBox" BasedOn="{StaticResource InputField}"/>

    <Style x:Key="PasswordField" TargetType="PasswordBox">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#FF42A5F5"/>
        <Setter Property="BorderThickness" Value="3"/>
        <Setter Property="Foreground" Value="#FF424242"/>
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Padding" Value="15,0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Height" Value="50"/>
        <Setter Property="Width" Value="350"/>
        <Setter Property="CaretBrush" Value="#FFC107"/>
        <Setter Property="PasswordChar" Value="●"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="PasswordBox">
                    <Grid>
                        <Border x:Name="shadow"
                            CornerRadius="25"
                            Background="#FF1565C0" 
                            Margin="0,4,0,0"
                            Opacity="0.6"/>

                        <Border x:Name="border"
                            CornerRadius="25"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Margin="0,0,0,4">

                            <ScrollViewer x:Name="PART_ContentHost" 
                                      Margin="0"
                                      VerticalAlignment="Center"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#F0F8FF"/>
                        </Trigger>

                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="BorderBrush" Value="#FFC107"/>
                            <Setter TargetName="border" Property="Background" Value="#FFFDE7"/>
                            <Setter TargetName="shadow" Property="Background" Value="#FFA000"/>
                            <Setter TargetName="shadow" Property="Opacity" Value="0.8"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="#EEEEEE"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#BDBDBD"/>
                            <Setter Property="Foreground" Value="Gray"/>
                            <Setter TargetName="shadow" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="border" Property="Margin" Value="0,4,0,0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GenderRadioButtonStyle" TargetType="RadioButton">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#CFD8DC"/>
        <Setter Property="Foreground" Value="#FF546E7A"/>
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Height" Value="45"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Margin" Value="5"/>

        <Setter Property="SnapsToDevicePixels" Value="True"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid RenderTransformOrigin="0.5,0.5">

                        <Border x:Name="shadow"
                            CornerRadius="22.5"
                            Background="{TemplateBinding BorderBrush}" 
                            Margin="0,4,0,0"/>

                        <Border x:Name="face"
                            CornerRadius="22.5"
                            Background="{TemplateBinding Background}"
                            Margin="0,0,0,4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"
                                          Margin="15,0"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="face" Property="Background" Value="#F5F5F5"/>
                        </MultiTrigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="face" Property="Margin" Value="0,4,0,0"/>
                            <Setter TargetName="shadow" Property="Visibility" Value="Hidden"/>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="#FFC107"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter TargetName="face" Property="Margin" Value="0,4,0,0"/>
                            <Setter TargetName="shadow" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AvatarFrame3D" TargetType="ContentControl">
        <Setter Property="Width" Value="160"/>
        <Setter Property="Height" Value="160"/>
        <Setter Property="IsTabStop" Value="False"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <Grid>
                        <Border Background="White"
                            BorderBrush="#FF333333" 
                            BorderThickness="4"
                            CornerRadius="80">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="4" Color="Black" Opacity="0.4" BlurRadius="10"/>
                            </Border.Effect>
                        </Border>

                        <Border Margin="4" 
                            Background="White" 
                            CornerRadius="76">

                            <Border.OpacityMask>
                                <VisualBrush>
                                    <VisualBrush.Visual>
                                        <Border Width="152" Height="152" 
                                            Background="Black" 
                                            CornerRadius="76"/>
                                    </VisualBrush.Visual>
                                </VisualBrush>
                            </Border.OpacityMask>

                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="EditButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#FFEE58"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Width" Value="45"/>
        <Setter Property="Height" Value="45"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid RenderTransformOrigin="0.5,0.5">

                        <Border x:Name="shadow"
                            CornerRadius="25"
                            Background="#FBC02D" 
                            Margin="0,4,0,0"/>

                        <Border x:Name="face"
                            CornerRadius="25"
                            Background="{TemplateBinding Background}"
                            Margin="0,0,0,4">

                            <Image Source="/Resources/Images/edit.png" 
                               Stretch="Uniform" 
                               Margin="11"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="face" Property="Background" Value="#FFF59D"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="face" Property="Margin" Value="0,4,0,0"/>
                            <Setter TargetName="shadow" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SidebarButtonBase" TargetType="Button">
        <Setter Property="Width" Value="55"/>
        <Setter Property="Height" Value="55"/>
        <Setter Property="Margin" Value="0,8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid RenderTransformOrigin="0.5,0.5">
                        <Border x:Name="shadow"
                                CornerRadius="18"
                                Background="{TemplateBinding BorderBrush}"
                                Margin="0,4,0,0"/>

                        <Border x:Name="face"
                                CornerRadius="18"
                                Background="{TemplateBinding Background}"
                                Margin="0,0,0,4">

                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="face" Property="Opacity" Value="0.9"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="face" Property="Margin" Value="0,4,0,0"/>
                            <Setter TargetName="shadow" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="SettingsButton" TargetType="Button" BasedOn="{StaticResource SidebarButtonBase}">
        <Setter Property="Background" Value="#E0E0E0"/>
        <Setter Property="BorderBrush" Value="#9E9E9E"/>
    </Style>
    <Style x:Key="FriendsButton" TargetType="Button" BasedOn="{StaticResource SidebarButtonBase}">
        <Setter Property="Background" Value="#81D4FA"/>
        <Setter Property="BorderBrush" Value="#0288D1"/>
    </Style>
    <Style x:Key="EditProfileButton" TargetType="Button" BasedOn="{StaticResource SidebarButtonBase}">
        <Setter Property="Background" Value="#F48FB1"/>
        <Setter Property="BorderBrush" Value="#C2185B"/>
    </Style>
    <Style x:Key="ChatButton" TargetType="Button" BasedOn="{StaticResource SidebarButtonBase}">
        <Setter Property="Background" Value="#A5D6A7"/>
        <Setter Property="BorderBrush" Value="#388E3C"/>
    </Style>
    
    <Style x:Key="ToggleSwitchStyle" TargetType="ToggleButton">
        <Setter Property="Width" Value="60"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid>
                        <Border x:Name="Track"
                            CornerRadius="16" 
                            Background="#CFD8DC" 
                            BorderBrush="#B0BEC5"
                            BorderThickness="2">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="0" BlurRadius="5" Color="Black" Opacity="0.1"/>
                            </Border.Effect>
                        </Border>

                        <Grid x:Name="ThumbContainer" HorizontalAlignment="Left">
                            <Border x:Name="Thumb" 
                                Width="24" Height="24" 
                                Background="White" 
                                CornerRadius="12" 
                                Margin="4,0">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="2" Color="#555" Opacity="0.3" BlurRadius="3"/>
                                </Border.Effect>
                            </Border>
                        </Grid>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Track" Property="Background" Value="#FF2ECC71"/>
                            <Setter TargetName="Track" Property="BorderBrush" Value="#FF27AE60"/>
                            <Setter TargetName="ThumbContainer" Property="HorizontalAlignment" Value="Right"/>
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Thumb" Property="Background" Value="#F5F5F5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SliderThumbStyle" TargetType="Thumb">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Height" Value="26"/>
        <Setter Property="Width" Value="26"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Grid>
                        <Border Width="26" Height="26" CornerRadius="13" Background="Black" Opacity="0.2" Margin="2,2,0,0">
                            <Border.Effect>
                                <BlurEffect Radius="3"/>
                            </Border.Effect>
                        </Border>
                        <Border Width="26" Height="26" CornerRadius="13" Background="White" BorderBrush="#CFD8DC" BorderThickness="1"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="VolumeSliderStyle" TargetType="Slider">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid VerticalAlignment="Center">

                        <Border Name="TrackBackground"
                            Height="10"
                            Background="#CFD8DC"
                            CornerRadius="5"
                            BorderBrush="#B0BEC5"
                            BorderThickness="1"
                            VerticalAlignment="Center"
                            Margin="5,0"/>

                        <Track x:Name="PART_Track">

                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="{x:Static Slider.DecreaseLarge}">
                                    <RepeatButton.Style>
                                        <Style TargetType="RepeatButton">
                                            <Setter Property="OverridesDefaultStyle" Value="True"/>
                                            <Setter Property="Focusable" Value="False"/>
                                            <Setter Property="IsTabStop" Value="False"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RepeatButton">
                                                        <Border Height="10" 
                                                            Background="#FF42A5F5" 
                                                            CornerRadius="5" 
                                                            VerticalAlignment="Center"/>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RepeatButton.Style>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>

                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="{x:Static Slider.IncreaseLarge}">
                                    <RepeatButton.Style>
                                        <Style TargetType="RepeatButton">
                                            <Setter Property="OverridesDefaultStyle" Value="True"/>
                                            <Setter Property="Focusable" Value="False"/>
                                            <Setter Property="IsTabStop" Value="False"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RepeatButton">
                                                        <Border Background="Transparent"/>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RepeatButton.Style>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>

                            <Track.Thumb>
                                <Thumb Style="{StaticResource SliderThumbStyle}" VerticalAlignment="Center"/>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="LanguageRadioButtonStyle" TargetType="RadioButton" BasedOn="{StaticResource GenderRadioButtonStyle}">
        <Setter Property="Padding" Value="20,5"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <Style x:Key="RedButtonStyle" TargetType="Button" BasedOn="{StaticResource MenuButtonBase}">
        <Setter Property="Background" Value="#D9534F"/>
        <Setter Property="Width" Value="Auto"/>
    </Style>

    <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#FF42A5F5"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Foreground" Value="#333"/>
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="10,0"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid>
                        <Border x:Name="shadow"
                            CornerRadius="20"
                            Background="#FF1565C0" 
                            Margin="0,3,0,0"
                            Opacity="0.3"/>

                        <Border x:Name="border"
                            CornerRadius="20"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Margin="0,0,0,3">

                            <Grid>
                                <TextBlock x:Name="Placeholder" 
                                       Text="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" 
                                       Foreground="#AAA" 
                                       Margin="10,0" 
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       FontStyle="Italic">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="0"
                                          VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#F0F8FF"/>
                        </Trigger>

                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="BorderBrush" Value="#FFC107"/>
                            <Setter TargetName="border" Property="Background" Value="#FFFDE7"/>
                            <Setter TargetName="shadow" Property="Background" Value="#FFA000"/>
                            <Setter TargetName="shadow" Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BlueIconButton" TargetType="Button">
        <Setter Property="Background" Value="#FF42A5F5"/>
        <Setter Property="Cursor" Value="Hand"/>

        <Setter Property="Width" Value="40"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Margin" Value="5,0,0,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid RenderTransformOrigin="0.5,0.5">

                        <Border x:Name="shadow"
                            CornerRadius="12"
                            Background="#FF1565C0" 
                            Margin="0,4,0,0"/>

                        <Border x:Name="face"
                            CornerRadius="12"
                            Background="{TemplateBinding Background}"
                            Margin="0,0,0,4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="face" Property="Background" Value="#FF64B5F6"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="face" Property="Margin" Value="0,4,0,0"/>
                            <Setter TargetName="shadow" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="DialogWindowStyle" TargetType="Window">
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="AllowsTransparency" Value="True"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="ResizeMode" Value="NoResize"/>
    </Style>

    <Style x:Key="CloseButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="30"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="Background" Value="#FF5C5C"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="Black"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid RenderTransformOrigin="0.5,0.5">

                        <Border x:Name="shadow"
                            CornerRadius="15"
                            Background="#B71C1C" 
                            Margin="0,3,0,0"/>

                        <Border x:Name="face"
                            CornerRadius="15"
                            Background="{TemplateBinding Background}"
                            Margin="0,0,0,3">

                            <TextBlock Text="X"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="{TemplateBinding Foreground}"
                                   Margin="0,0,0,2"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="face" Property="Background" Value="#FF7979"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="face" Property="Margin" Value="0,3,0,0"/>
                            <Setter TargetName="shadow" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="PodiumNumberStyle" TargetType="Label">
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="72"/>
        <Setter Property="FontWeight" Value="ExtraBold"/>
        <Setter Property="Foreground" Value="Blue"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect ShadowDepth="5"
                              Direction="315"
                              Color="White"
                              Opacity="0.6"
                              BlurRadius="8"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ToolButton" TargetType="RadioButton">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#B0BEC5"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid RenderTransformOrigin="0.5,0.5">
                        <Grid.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                        </Grid.RenderTransform>

                        <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"
                                          Margin="4"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="border" Property="BorderBrush" Value="#FF42A5F5"/>
                            <Setter TargetName="border" Property="Background" Value="#F0F8FF"/>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="#FFC107"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="3"/>
                            <Setter TargetName="border" Property="Background" Value="#FFFDE7"/>
                            <Setter TargetName="border" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect ShadowDepth="0" Color="#FFA000" BlurRadius="10" Opacity="0.8"/>
                                </Setter.Value>
                            </Setter>

                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1.15" ScaleY="1.15"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="ColorPaletteStyle" TargetType="RadioButton">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid RenderTransformOrigin="0.5,0.5">
                        <Grid.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                        </Grid.RenderTransform>

                        <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent"
                            BorderThickness="2"
                            CornerRadius="10">
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="border" Property="BorderBrush" Value="#88FFFFFF"/>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="#FFC107"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="3"/>
                            <Setter TargetName="border" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect ShadowDepth="0" Color="#FFA000" BlurRadius="10" Opacity="0.8"/>
                                </Setter.Value>
                            </Setter>

                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1.15" ScaleY="1.15"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="ReturnButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#90A4AE"/>
        <Setter Property="Foreground" Value="#546E7A"/>
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="FontSize" Value="28"/>
        <Setter Property="FontWeight" Value="ExtraBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Width" Value="50"/>
        <Setter Property="Height" Value="50"/>
        <Setter Property="Margin" Value="15,15,0,0"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid RenderTransformOrigin="0.5,0.5">

                        <Border x:Name="shadow"
                            CornerRadius="25"
                            Background="{TemplateBinding BorderBrush}" 
                            Margin="0,6,0,0"/>
                        <Border x:Name="face"
                            CornerRadius="25"
                            Background="{TemplateBinding Background}"
                            Margin="0,0,0,6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"
                                          Margin="0,0,0,4"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="face" Property="Background" Value="#F5F5F5"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="face" Property="Margin" Value="0,6,0,0"/>
                            <Setter TargetName="shadow" Property="Visibility" Value="Collapsed"/>
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <TranslateTransform Y="1"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="ThicknessSlider" TargetType="{x:Type Slider}">
        <Setter Property="Height" Value="22"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Slider}">
                    <Grid VerticalAlignment="Center">

                        <Border x:Name="TrackBackground"
                            Background="#E0E0E0" 
                            BorderBrush="Black"
                            BorderThickness="1.5"
                            Height="14"
                            CornerRadius="7"/>
                        <Track x:Name="PART_Track">

                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="{x:Static Slider.DecreaseLarge}">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Height="14"
                                                CornerRadius="7,0,0,7"
                                                Background="Black"/>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>

                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Background="Transparent">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Background="Transparent"/>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>

                            <Track.Thumb>
                                <Thumb x:Name="Thumb" Width="22" Height="22">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Ellipse x:Name="thumbCircle"
                                                 Fill="White"
                                                 Stroke="Black"
                                                 StrokeThickness="1.5"/>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="thumbCircle" Property="Fill" Value="#F0F0F0"/>
                                                </Trigger>
                                                <Trigger Property="IsDragging" Value="True">
                                                    <Setter TargetName="thumbCircle" Property="Fill" Value="#DDDDDD"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>

                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TextBlockInkCanvas" TargetType="Label">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="5,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Label">
                    <Border CornerRadius="8"
                        BorderBrush="Black"
                        BorderThickness="3"
                        Background="White"
                        Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DrawingDisplayBorder" TargetType="Border">
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="BorderBrush" Value="Black"/>
        <Setter Property="BorderThickness" Value="3"/>
        <Setter Property="Background" Value="White"/>
    </Style>

    <Style x:Key="TimerStyle" TargetType="Label">

        <Setter Property="Background" Value="#FFFDE7"/>
        <Setter Property="BorderBrush" Value="#FFC107"/>
        <Setter Property="Foreground" Value="#E65100"/>
        <Setter Property="FontFamily" Value="Arial Rounded MT Bold"/>
        <Setter Property="FontSize" Value="40"/>
        <Setter Property="FontWeight" Value="Black"/>
        <Setter Property="Padding" Value="25,5"/>
        <Setter Property="VerticalAlignment" Value="Center"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Label">
                    <Grid>
                        <Border CornerRadius="30" Background="Transparent" Margin="2,4,2,0">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="0" 
                                              Color="#FFFF8F00" 
                                              Opacity="0.4" 
                                              BlurRadius="15"/>
                            </Border.Effect>
                        </Border>

                        <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="3" 
                            CornerRadius="30"
                            Padding="{TemplateBinding Padding}">

                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                                <TextBlock Text="⏰" 
                                       FontSize="32" 
                                       VerticalAlignment="Center" 
                                       Margin="0,0,12,2"
                                       Foreground="#FFD84315" 
                                       RenderTransformOrigin="0.5,0.5">
                                    <TextBlock.RenderTransform>
                                        <RotateTransform Angle="-12"/>
                                    </TextBlock.RenderTransform>
                                </TextBlock>

                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>